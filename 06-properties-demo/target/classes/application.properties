spring.application.name=mycoolapp
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

#Exclude individual endpoints with a comma list
#management.endpoints.web.exposure.exclude=health,info


info.app.name = Allan's Super Cool App
info.app.description = First Fullstack App WOO!
info.app.version = 1.0.0

#Define my own properties (not so crazy)
coach.name = Sheldon Keefe
team.name = NJ Devils

# Change Spring boot embedded server port
server.port = 7070

# Set the context path of the application
# All requests should be prefixed with /mycoolapp
server.servlet.context-path=/mycoolapp

